import { FiltreReservesPipe } from './filtre-reserves.pipe';

describe('FiltreReservesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreReservesPipe();
    expect(pipe).toBeTruthy();
  });
});
